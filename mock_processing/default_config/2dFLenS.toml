mask = "samples/2dFLenS"
                        # path at which the sample selection bit mask is saved
                        # in the data store

[magnitudes]
mag_g = "..."           # path of g-band magnitude in the data store
mag_r = "..."           # path of r-band magnitude in the data store
mag_i = "..."           # path of i-band magnitude in the data store
mag_Z = "..."           # path of Z-band magnitude in the data store
mag_Ks = "..."          # path of Ks-band magnitude in the data store
